simulation:
  main_dir: /cosma8/data/dp004/colibre/Runs #do not touch!
  chimes_table_dir: /cosma8/data/dp004/jlvc76/COLIBRE/Tables/chimes-data/EqAbundancesTables/colibre_HHe/ #this is where the chimes tables are stored, change if necessary
  box_length: 12 #cMpc
  resolution: 6 # 10^resolution  M_solar 
  name: Thermal
  snapshot_number: 127 #can be a single int or list of ints

data_output:
  main_dir: /cosma8/data/do012/dc-kort1/CGM/ #this is where the I store my outputs, change if necessary
  results_dir: test 


window:
    x: [0.0, 1.0]
    y: [0.0, 1.0]
    z: [0.0, 0.5]
    resolution: 1000 #number of bins in the histogram (2D histogram is shape(resolutions,resolution))

chemistry:
  element: hydrogen
  ion: Hm

galaxy:
  # choose: random, highest_gas_mass, most_bound_particles,
  selection: most_bound_particles # choose: random, highest_gas_mass, most_bound_particles,
  

monitoring:
  cpu_ram_monitor: off
  monitor_interval: 240 #in seconds
